package utils;

public class Constants {
    public static enum prioridadEnum { alta, media, baja };
    public static enum tipoAyudaEnum { medica, donacion, otros };
    public static enum estadoEnum { aprobado, rechazado, pendiente };
}
